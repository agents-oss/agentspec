apiVersion: agentspec.io/v1
kind: AgentObservation
metadata:
  name: voice-assistant
  namespace: demo
  labels:
    agentspec.io/agent: voice-assistant
    agentspec.io/profile: degraded   # C-grade: no guardrails, no /health, no /capabilities; → A after demo-patch
spec:
  agentRef:
    name: voice-assistant
  sidecarServiceName: voice-assistant-sidecar
  sidecarPort: 4001
  checkInterval: 30
