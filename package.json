{
  "name": "agentspec",
  "version": "0.0.0",
  "private": true,
  "description": "Universal Agent Manifest System — spec, SDK, and CLI",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck",
    "schema:export": "tsx packages/sdk/src/scripts/export-schema.ts"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "tsup": "^8.3.5",
    "vitepress": "^1.6.3"
  }
}
